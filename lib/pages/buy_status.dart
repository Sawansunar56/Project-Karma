import 'package:flutter/material.dart';

class BuyStatusPage extends StatelessWidget {
  const BuyStatusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Buy Status"),
      ),
      body: Column(children: []),
    );
  }
}
